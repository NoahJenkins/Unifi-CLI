# Auto detect text files and perform LF normalization
* text=auto

# Keep formatter-checked source and automation files identical on every OS.
*.go text eol=lf
*.sh text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
